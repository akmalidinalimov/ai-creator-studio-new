// TeacherHome — `/tg/teacher` home screen.
// Task 3 placeholder; Task 5 fleshes this out (Hero + StatTile grid + Baholash hero CTA + states).
export default function TeacherHome() {
  return <div className="p-4 text-foreground">Teacher Home</div>;
}
